{
  "name": "crm-portal-mock",
  "version": "1.0.0",
  "description": "Salesforce-styled mock CRM portal showing a list of customer requests (External ID, description, timestamp).",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node --watch src/app.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "express": "^4.18.0"
  }
}
